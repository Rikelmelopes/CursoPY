frase = "Curso em Video Python"
print(len(frase))

#frase.replace("python","android"),usado para substituir
#frase.upper() = deixa tudo maisculo
#frase.lower() = deixa tudo minusculo, o que estava maisculo 
#frase.capitalize() = deixa tudo minusculo , menos a primeira letra da frase
#frase.title() =quantas palavras a frase tem , e cada uma delas fica com a letra maiuscula
#frase.strip() = remove os todos espaços inuteis
#frase.rstrip() = remove so os ultimos espaços (direita)
#frase.lstrip() = remove os espaços do começo (esquerda)
#frase.split() = gera uma lista em todas as palavras
#"-".join(frase) = junta as as palavras que o splite separou em lista

#aspas triplas (''')são usadas para um print em algo com mais de uma linha

